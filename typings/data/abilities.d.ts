import { models } from '../../typings';
export declare function Attack(damage: number, effects?: string[]): models.server.Ability;
export declare function Heal(damage: number, effects?: string[]): models.server.Ability;
